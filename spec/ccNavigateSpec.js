describe('cc-navigate', function () {

	//TODO test the directive
	
    it('should navigate to home', function () {
        expect(canary()).toEqual(true);
    });

    it('should navigate to play', function () {
        expect(canary()).toEqual(true);
    });
});